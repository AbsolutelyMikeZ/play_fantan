<h1>Start New Game</h1>

<%= form_for(@game) do |f| %>
<div class="field">
    Total Players for Game: 
    <%= f.select(:num_players, [3, 4, 5, 6, 7, 8], :selected => 4) %>
  </div>
  <div class="actions">
    <%= f.submit "Start", class: "green btn" %>
  </div>
<% end %>